<%= form_for(@play, url: play_path) do |f| %>
  <% if @play.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(play.errors.count, "error") %> prohibited this play from being saved:</h2>

      <ul>
      <% @play.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= @play.points %>
  </div>
  <div class="field">
    <%= f.label :bet %>
    <%= f.number_field :bet, id: :play_bet %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
